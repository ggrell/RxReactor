name: Publish a release

on:
  release:
    types: [released]

jobs:
  build:
    name: Build, run checks, release to Maven Central
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      BUNDLE_GEMFILE: ${{ github.workspace }}/.github/Gemfile
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
          cache: 'gradle'
          cache-dependency-path: |
            gradle/libs.versions.toml
            gradle.properties
            settings.gradle
            build.gradle
            */build.gradle
      - name: Cache Gradle wrapper distributions
        uses: actions/cache@v4
        with:
          path: ~/.gradle/wrapper/dists
          key: ${{ runner.os }}-gradle-wrapper-dists-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-wrapper-dists-
      - name: Cache Android SDK packages
        uses: actions/cache@v4
        with:
          path: |
            /usr/local/lib/android/sdk/build-tools/36.0.0
            /usr/local/lib/android/sdk/platforms/android-36
            /usr/local/lib/android/sdk/platform-tools
            /usr/local/lib/android/sdk/licenses
          key: ${{ runner.os }}-android-sdk-36-36.0.0-v1
          restore-keys: |
            ${{ runner.os }}-android-sdk-
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.2
          bundler-cache: true
      - name: Assemble release
        run: |
          echo "::add-matcher::.github/gradle-java-matcher.json"
          echo "::add-matcher::.github/kotlin-error-matcher.json"
          echo "::add-matcher::.github/kotlin-warning-matcher.json"
          ./gradlew assembleRelease --no-daemon -Prelease=true
      - name: Run all checks and create coverage report
        run: |
          ./gradlew check jacocoTestReport --no-daemon -Prelease=true
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
      - name: Publish to Maven Central Releases repo
        env:
          ORG_GRADLE_PROJECT_signingKeyId: ${{ secrets.MAVEN_CENTRAL_KEY_ID }}
          ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingKey: ${{ secrets.MAVEN_CENTRAL_KEY }}
          OSSRH_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          OSSRH_PASSWORD: ${{ secrets.OSSRH_PASSWORD }}
        run: |
          ./gradlew publishAllPublicationsToMavenCentralReleasesRepository -Prelease=true
      - name: Add artifacts to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            **/rxreactor*.?ar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
